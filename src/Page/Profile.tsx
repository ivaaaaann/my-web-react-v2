import ProfileForm from "../Components/Profile/ProfileForm";

const Profile: React.FC = () => {
  return (
    <section id="Profile">
      <ProfileForm />
    </section>
  );
};

export default Profile;
